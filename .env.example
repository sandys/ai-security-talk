# Copy to .env (git-ignored) if you want to point labs at optional services.
# NO lab requires any of these. Never commit real credentials.

# Module 08: ship spans to Phoenix / any OTLP-HTTP collector (unset = in-memory only)
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:6006/v1/traces

# If you later replace the deterministic agent with a real model, keep the key here,
# never in a notebook cell:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
